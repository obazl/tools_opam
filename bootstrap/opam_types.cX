#if EXPORT_INTERFACE
#include <stdbool.h>
union opam_token_u {
    char c;
    int i;
    char *s;
    bool boolval;
};

enum token_type_e {
    BINDING_BOOL,
    BINDING_INT,
    BINDING_STRING,
};

#endif
