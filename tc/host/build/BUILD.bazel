platform(name = "sys",
         parents = ["@local_config_platform//:host"],
         constraint_values = [
             "@ocaml//host/build:sys",
         ]
         )

platform(name = "vm",
         parents = ["@local_config_platform//:host"],
         constraint_values = [
             "@ocaml//host/build:vm",
         ])

