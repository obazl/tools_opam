platform(name = "local",
         parents = ["@local_config_platform//:host"],
         # constraint_values = [
         #     "@ocaml//host/build:native",
         # ]
         )

platform(name = "vm",
         parents = ["@local_config_platform//:host"],
         constraint_values = [
             "@ocaml//toolchain/platform_constraints/build:vm",
         ])

